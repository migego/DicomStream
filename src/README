A) File Prefetch

- blocking queue with list<Iterator<string>>
- iterator can be an UpDownIterator, or a FragmentIterator

- ability to re-prioritize the list:
      - push one iterator to top of list
      - reset primary frame
      
 Need to tests if posix_fadvise works for CIFS mount:
    On Linux
    
    Show Free Memory:
        $  free -m
    
    Clear disk cache:
       $  echo 3 | sudo tee /proc/sys/vm/drop_caches 
       
     Time to cat a file name "bigfile"
       $ time cat bigfile > /dev/null
       
       http://www.linuxatemyram.com/play.html
       
B) Get libeio working for file reads

C) modify gdcmStreamImageReader to read from memory buffer
    - handle multi-frame       
      - 
